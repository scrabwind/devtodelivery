datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model People {
  id         String  @id @default(uuid())
  height     Int?
  mass       Int?
  hair_color String? @default("n/a")
}
